<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Unibox"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".RegisterActivity" />

        <activity
            android:name=".EditProductSeller"
            android:exported="false" />
        <activity
            android:name=".DetailsCajitaSeller"/>

        <activity
            android:name=".AddProductSellerActivity"
            android:exported="false" />

        <activity android:name=".HomeSellerActivity" />

        <activity android:name=".MisCajitasSellerActivity" />

        <activity android:name=".HomeClientActivity" />

        <activity android:name=".MapClientActivity" />

        <activity android:name=".ListDaniPostresClientActivity" />

        <activity android:name=".SearchAllClientActivity" />

        <activity android:name=".SearchBClientActivity"/>

        <activity android:name=".ViewGeneralClientActivity"/>

        <activity android:name=".UserProfileClient" />

        <activity android:name=".UpdateUserProfileActivity" />
    </application>

</manifest>